<UserControl
	x:Class="TombEditor.WPF.ToolWindows.Palette"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:DarkUI.WPF.CustomControls;assembly=DarkUI.WPF"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:wpfControls="clr-namespace:TombEditor.WPF.Controls"
	d:DesignHeight="120"
	d:DesignWidth="645"
	Background="{DynamicResource Brush_Background}"
	RenderOptions.BitmapScalingMode="NearestNeighbor"
	mc:Ignorable="d">

	<controls:SpacedGrid Margin="{x:Static darkUI:Defaults.SmallThickness}" ColumnSpacing="{x:Static darkUI:Defaults.SmallThicknessWidth}">
		<Grid.LayoutTransform>
			<ScaleTransform x:Name="Scaler" ScaleX="1" ScaleY="1" />
		</Grid.LayoutTransform>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<controls:SpacedStack Spacing="{x:Static darkUI:Defaults.SmallThicknessWidth}">
			<Button
				x:Name="butResetToDefaults"
				Command="{Binding ResetPaletteCommand}"
				IsEnabled="{Binding CanResetPalette}"
				Style="{DynamicResource ButtonWithIconOnly}">
				<Image Source="/TombEditor;component/Resources/icons_actions/actions_refresh-16.png" />
			</Button>
			<ToggleButton
				x:Name="butSampleFromTextures"
				Command="{Binding SampleFromTexturesCommand}"
				IsChecked="{Binding IsTextureSamplingMode, Mode=OneWay}"
				Style="{DynamicResource ToggleButtonWithIconOnly}">
				<Image Source="/TombEditor;component/Resources/icons_actions/actions_TextureMode-16.png" />
			</ToggleButton>
			<Button
				x:Name="butEditColor"
				Command="{Binding EditObjectColorCommand}"
				IsEnabled="{Binding CanEditColor}"
				Style="{DynamicResource ButtonWithIconOnly}">
				<Image Source="/TombEditor;component/Resources/icons_general/general_edit-16.png" />
			</Button>
		</controls:SpacedStack>

		<wpfControls:PanelPaletteGrid
			x:Name="PaletteGrid"
			Grid.Column="1"
			LayoutTransform="{Binding LayoutTransform.Inverse, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}" />
	</controls:SpacedGrid>
</UserControl>
