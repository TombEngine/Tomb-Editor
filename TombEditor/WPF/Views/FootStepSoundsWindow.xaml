<Window
	x:Class="TombEditor.WPF.Views.FootStepSoundsWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:darkCtrl="clr-namespace:DarkUI.WPF.CustomControls;assembly=DarkUI.WPF"
	xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
	xmlns:local="clr-namespace:TombEditor.WPF"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	
	Title="{tombLib:Localize TextureSoundsTitle}"
	Width="440"
	Height="601"
	MinWidth="440"
	MinHeight="601"
	Style="{DynamicResource CustomWindowStyle}"
	mc:Ignorable="d">
	<controls:SpacedGrid Margin="{x:Static darkUI:Defaults.SmallThickness}">
		<controls:SpacedGrid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
			<RowDefinition Height="Auto" />
		</controls:SpacedGrid.RowDefinitions>
		<darkCtrl:SearchableComboBox />
		<Border
			Grid.Row="1"
			BorderBrush="#FF5C5C5C"
			BorderThickness="1">
			<WindowsFormsHost x:Name="ViewportHost" />
		</Border>

		<controls:SpacedGrid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<darkCtrl:SearchableComboBox>
				<ComboBoxItem Content="{tombLib:Localize Mud}" />
				<ComboBoxItem Content="{tombLib:Localize Snow}" />
				<ComboBoxItem Content="{tombLib:Localize Sand}" />
				<ComboBoxItem Content="{tombLib:Localize Gravel}" />
				<ComboBoxItem Content="{tombLib:Localize Ice}" />
				<ComboBoxItem Content="{tombLib:Localize Water}" />
				<ComboBoxItem Content="{tombLib:Localize Stone}" />
				<ComboBoxItem Content="{tombLib:Localize Wood}" />
				<ComboBoxItem Content="{tombLib:Localize Metal}" />
				<ComboBoxItem Content="{tombLib:Localize Marble}" />
				<ComboBoxItem Content="{tombLib:Localize Grass}" />
				<ComboBoxItem Content="{tombLib:Localize Concrete}" />
				<ComboBoxItem Content="{tombLib:Localize OldWood}" />
				<ComboBoxItem Content="{tombLib:Localize OldMetal}" />
				<ComboBoxItem Content="{tombLib:Localize Custom1}" />
				<ComboBoxItem Content="{tombLib:Localize Custom2}" />
				<ComboBoxItem Content="{tombLib:Localize Custom3}" />
				<ComboBoxItem Content="{tombLib:Localize Custom4}" />
				<ComboBoxItem Content="{tombLib:Localize Custom5}" />
				<ComboBoxItem Content="{tombLib:Localize Custom6}" />
				<ComboBoxItem Content="{tombLib:Localize Custom7}" />
				<ComboBoxItem Content="{tombLib:Localize Custom8}" />
			</darkCtrl:SearchableComboBox>
			<Button
				Grid.Column="1"
				Width="113"
				Content="{tombLib:Localize AssignSound}" />
			<Button
				Grid.Column="2"
				Width="80"
				Content="{tombLib:Localize OK}"
				IsDefault="True" />
		</controls:SpacedGrid>
	</controls:SpacedGrid>
</Window>
