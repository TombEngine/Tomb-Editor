<Window
    x:Class="TombEditor.WPF.Views.FindTexturesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:darkUI="clr-namespace:DarkUI.WPF;assembly=DarkUI.WPF"
    xmlns:local="clr-namespace:TombEditor.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    Title="{tombLib:Localize FindTexturesTitle}"
    Width="330"
    Height="400"
    MinWidth="300"
    MinHeight="400"
    Style="{DynamicResource CustomWindowStyle}"
    mc:Ignorable="d">
    <controls:SpacedGrid Margin="{x:Static darkUI:Defaults.SmallThickness}">
        <controls:SpacedGrid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </controls:SpacedGrid.RowDefinitions>
        <DataGrid ScrollViewer.VerticalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Header="{tombLib:Localize FindTexturesGridColRoom}" />
                <DataGridTextColumn Width="70" Header="{tombLib:Localize FindTexturesGridBlock}" />
            </DataGrid.Columns>
        </DataGrid>
        <controls:SpacedGrid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Label Content="{tombLib:Localize FindTexturesFind}" />
            <ComboBox Grid.Column="1">
                <ComboBoxItem Content="{tombLib:Localize UntexturedFaces}" />
                <ComboBoxItem Content="{tombLib:Localize InvisibleFaces}" />
                <ComboBoxItem Content="{tombLib:Localize TexturesMissing}" />
                <ComboBoxItem Content="{tombLib:Localize ExactMatch}" />
                <ComboBoxItem Content="{tombLib:Localize PartialMatch}" />
                <ComboBoxItem Content="{tombLib:Localize TextureSetOnly}" />
            </ComboBox>
        </controls:SpacedGrid>
        <controls:SpacedGrid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <CheckBox Content="{tombLib:Localize SelectedRooms}" />
            <Button
                Grid.Column="1"
                Width="80"
                Content="{tombLib:Localize NewSearch}" />
            <Button
                Grid.Column="2"
                Width="80"
                Content="{tombLib:Localize Close}"
                IsDefault="True" />
        </controls:SpacedGrid>
        <StatusBarItem Grid.Row="3">
            <TextBlock />
        </StatusBarItem>
    </controls:SpacedGrid>
</Window>
